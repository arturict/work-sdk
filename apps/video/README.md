# Work SDK v0.5 launch video

An original 30-second, 1280×720 Remotion product teaser for Work SDK v0.5.

```bash
pnpm --filter @work-sdk/video dev
pnpm --filter @work-sdk/video still
pnpm --filter @work-sdk/video render
```

The company marks under `public/brands` are the same locally cached SVGL assets used by the marketing site. Attribution links are recorded in `public/brands/SOURCES.md`.
